origins:
  pumpkin_hate:
    # Poison the player when consuming pumpkin pie rather than preventing them from eating it
    consume_and_poison: false
  weak_arms:
    # Blocks that count as natural stone
    natural_stones:
    - minecraft:stone
    - minecraft:tuff
    - minecraft:granite
    - minecraft:diorite
    - minecraft:andesite
    - minecraft:sandstone
    - minecraft:smooth_sandstone
    - minecraft:red_sandstone
    - minecraft:smooth_red_sandstone
    - minecraft:deepslate
    - minecraft:blackstone
    - minecraft:netherrack
  fragile:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  fresh_air:
    # Minimum altitude the player can sleep at
    minimum_altitude: 86
  vegetarian:
    # Poison the player when consuming meat rather than preventing them from eating it
    consume_and_poison: false
    # Items that count as meat
    meat:
    - minecraft:porkchop
    - minecraft:cooked_porkchop
    - minecraft:beef
    - minecraft:cooked_beef
    - minecraft:chicken
    - minecraft:cooked_chicken
    - minecraft:rabbit
    - minecraft:cooked_rabbit
    - minecraft:mutton
    - minecraft:cooked_mutton
    - minecraft:rabbit_stew
    - minecraft:cod
    - minecraft:cooked_cod
    - minecraft:tropical_fish
    - minecraft:salmon
    - minecraft:cooked_salmon
    - minecraft:pufferfish
    - minecraft:rotten_flesh
    - minecraft:spider_eye
  master_of_webs:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 120
  tailwind:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  arthropod:
    # Apply Slowness when using Bane of Arthropods on players with the Arthropod ability
    apply_slowness: true
  carnivore:
    # Poison the player when consuming non-meat rather than preventing them from eating it
    consume_and_poison: false
    # Items that count as meat
    meat:
    - minecraft:porkchop
    - minecraft:cooked_porkchop
    - minecraft:beef
    - minecraft:cooked_beef
    - minecraft:chicken
    - minecraft:cooked_chicken
    - minecraft:rabbit
    - minecraft:cooked_rabbit
    - minecraft:mutton
    - minecraft:cooked_mutton
    - minecraft:rabbit_stew
    - minecraft:cod
    - minecraft:cooked_cod
    - minecraft:tropical_fish
    - minecraft:salmon
    - minecraft:cooked_salmon
    - minecraft:pufferfish
    - minecraft:rotten_flesh
    - minecraft:spider_eye
  nine_lives:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  burn_in_daylight:
    # Whether the player should burn without wearing a helmet
    burn_with_helmet: false
  water_vulnerability:
    # Amount of damage to take in water
    damage_amount: 1
  phantomize:
    # The food level at which you can no longer enter Phantom Form
    minimum_food_level: 6
    # Whether to disable Phantom Form when taking damage
    disable_on_damage: false
    trigger:
      # The action (e.g. LEFT_CLICK, RIGHT_CLICK) that triggers this ability
      # You can see a full list of actions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      type: LEFT_CLICK
      # The conditions (e.g. EMPTY_HAND, NO_BLOCK) that are required to trigger this ability
      # You can see a full list of conditions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      conditions:
      - NO_BLOCK
      - EMPTY_HAND
  throw_ender_pearl:
    trigger:
      # The action (e.g. LEFT_CLICK, RIGHT_CLICK) that triggers this ability
      # You can see a full list of actions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      type: LEFT_CLICK
      # The conditions (e.g. EMPTY_HAND, NO_BLOCK) that are required to trigger this ability
      # You can see a full list of conditions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      conditions:
      - NO_BLOCK
      - EMPTY_HAND
    # The duration of the cooldown (in ticks)
    cooldown-duration: 30
  air_from_potions:
    # Amount to increase air by when drinking a potion
    air_increase: 60
  light_armor:
    # Armor allowed to be equipped
    allowed_armor:
    - minecraft:chainmail_helmet
    - minecraft:chainmail_chestplate
    - minecraft:chainmail_leggings
    - minecraft:chainmail_boots
    - minecraft:leather_helmet
    - minecraft:leather_chestplate
    - minecraft:leather_leggings
    - minecraft:leather_boots
    - minecraft:golden_helmet
    - minecraft:golden_chestplate
    - minecraft:golden_leggings
    - minecraft:golden_boots
  damage_from_potions:
    # Amount of damage the player should take when drinking a potion
    damage_amount: 2
  damage_from_snowballs:
    # Amount of damage the player should take when hit with a snowball
    damage_amount: 3
    # Whether snowballs should deal knockback too
    knockback: true
  burning_wrath:
    # How much to increase damage dealt when on fire
    damage_increase: 3
  sprint_jump:
    # Strength of the Jump Boost effect to give
    jump_strength: 1
  aerial_combatant:
    # Amount to multiply the damage by when gliding
    damage_multiplier: 2.0
  elytra:
    # Disable the ability to disengage the elytra by double tapping again
    disable_disengage_trigger: true
  launch_into_air:
    # How strong the launch effect should be
    launch_strength: 2.0
    trigger:
      # The action (e.g. LEFT_CLICK, RIGHT_CLICK) that triggers this ability
      # You can see a full list of actions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      type: SNEAK_ON
      # The conditions (e.g. EMPTY_HAND, NO_BLOCK) that are required to trigger this ability
      # You can see a full list of conditions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      conditions:
      - GLIDING
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  hunger_over_time:
    # The amount to add to the player's exhaustion each second
    metabolism_increase: 0.812
  more_exhaustion:
    # Amount to multiply exhaustion by
    exhaustion_multiplier: 1.6
  aquatic:
    # Amount to increase damage by per level of Impaling
    impaling_damage_increase: 2.5
  flame_particles:
    # How often (in ticks) the particles should appear
    frequency: 4
  ender_particles:
    # How often (in ticks) the particles should appear
    frequency: 4
  phasing:
    # Blocks you cannot pass through when phasing
    impassable_blocks:
    - minecraft:obsidian
    - minecraft:crying_obsidian
    - minecraft:bedrock
    # Apply Blindness when Phasing
    give_blindness: true
    # Speed at which the player should move when phasing
    phasing_speed: 0.1
  strong_arms:
    # Blocks that count as natural stone
    natural_stones:
    - minecraft:stone
    - minecraft:tuff
    - minecraft:granite
    - minecraft:diorite
    - minecraft:andesite
    - minecraft:sandstone
    - minecraft:smooth_sandstone
    - minecraft:red_sandstone
    - minecraft:smooth_red_sandstone
    - minecraft:deepslate
    - minecraft:blackstone
    - minecraft:netherrack
    - minecraft:cobblestone
    - minecraft:cobbled_deepslate
    - minecraft:gold_ore
    - minecraft:iron_ore
    - minecraft:coal_ore
    - minecraft:copper_ore
    - minecraft:diamond_ore
    - minecraft:emerald_ore
    - minecraft:redstone_ore
    - minecraft:nether_gold_ore
    - minecraft:lapis_ore
    - minecraft:nether_quartz_ore
    - minecraft:deepslate_coal_ore
    - minecraft:deepslate_gold_ore
    - minecraft:deepslate_iron_ore
    - minecraft:deepslate_copper_ore
    - minecraft:deepslate_diamond_ore
    - minecraft:deepslate_emerald_ore
    - minecraft:deepslate_redstone_ore
    - minecraft:deepslate_lapis_ore
  shulker_inventory:
    # Drop items in the inventory on death (will not change ability description, for that edit the translations.yml file)
    drop_on_death: false
    trigger:
      # The action (e.g. LEFT_CLICK, RIGHT_CLICK) that triggers this ability
      # You can see a full list of actions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      type: RIGHT_CLICK_CHESTPLATE
      # The conditions (e.g. EMPTY_HAND, NO_BLOCK) that are required to trigger this ability
      # You can see a full list of conditions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      conditions: []
  natural_armor:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  conduit_power_on_land:
    # Whether to force compatibility with other abilities
    # Less efficient, but prevents it breaking if a player has both this and an ability like origins:water_vulnerability
    forced_compatibility: false
  extra_reach_blocks:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  extra_reach_entities:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  daylight_sensitive:
    # Whether the player should burn even when wearing a helmet
    burn_with_helmet: false
  undead:
    # Whether the player should burn even when wearing a helmet
    burn_with_helmet: false
  witch_particles:
    # How often (in ticks) the particles should appear
    frequency: 4
  burn_in_day:
    # Whether the player should burn even when wearing a helmet
    burn_with_helmet: false
fantasyorigins:
  bow_burst:
    # The number of arrows to fire
    arrow_count: 3
    # The time in seconds to disable the bow for
    cooldown_time: 7
  chime:
    # Time in ticks the regeneration effect should last
    regeneration_time: 900
  double_health:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  dragon_fireball:
    # The cooldown in seconds between each fireball
    cooldown_time: 30
    # The velocity the fireball should be given
    fireball_velocity: 1.2
    trigger:
      # The action (e.g. LEFT_CLICK, RIGHT_CLICK) that triggers this ability
      # You can see a full list of actions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      type: RIGHT_CLICK
      # The conditions (e.g. EMPTY_HAND, NO_BLOCK) that are required to trigger this ability
      # You can see a full list of conditions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      conditions:
      - HOLDING_SWORD
  elegy:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  end_crystal_healing:
    # Range the end crystal effect should be
    crystal_range: 48
  fortune_increaser:
    # Level to increase Fortune by
    fortune_increase: 2
  increased_arrow_damage:
    # Amount to add to arrow damage
    arrow_damage_increase: 3.0
  increased_arrow_speed:
    # Multiplier for the arrow's velocity
    arrow_speed_multiplier: 2.0
  increased_cooldown:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  increased_damage:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  end_health:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  end_strength:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  infinite_haste:
    # Level of the effect
    effect_level: 1
  land_weakness:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  land_health:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  land_slowness:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  moon_strength:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  natural_armor:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  water_health:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  water_strength:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  vampiric_transformation:
    # Chance (between 0 and 1) a player will transform into a vampire when killed by one
    transformation_chance: 1.0
  water_sensitive:
    # Amount to damage the player by
    damage_amount: 1
  stronger:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  solid_stance:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  stiff:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  large_body:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  small_body:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
magicorigins:
  kill_boost:
    # The maximum amount of extra health you can get
    max_boost: 60
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  cursed_strikes:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 900
  gold_converter:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 100
  unskilled_warrior:
    # Amount to multiply melee damage dealt by
    damage_multiplier: 0.75
  weak_in_water:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  control_monsters:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  interactions_give_nausea:
    # How long the Nausea effect shoud last
    effect_duration: 600
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  potion_master:
    # Number of levels to increase the potion strength by
    strength_increase: 1
    # Multiplier for the potion duration
    duration_multiplier: 2.0
  nether_strong:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  nether_health:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  spare_life:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 12000
  interact_regeneration:
    # How long the Regeneration effect shoud last
    effect_duration: 200
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  undead_commander:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  bring_back_dead:
    # Whether to apply the Totem of Undying effect when a player is brought back
    use_totem_effects: true
    # The duration of the cooldown (in ticks)
    cooldown-duration: 3600
  invisible_in_darkness:
    # The light level the player turns invisible in
    light_level: 4
    # Whether players in Shadow Form should be hidden from admins
    hide_from_administrators: true
  dark_strength:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  burn_in_light:
    # The light level that the player should begin to burn at
    light_level: 7
  extra_reach_blocks:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  extra_reach_entities:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
moborigins:
  small_bug:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  small_fox:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  lower_totem_chance:
    # The chance a totem won't break (between 0 and 1)
    no_break_chance: 0.1
  stronger_snowballs:
    # Should snowballs do knockback
    do_knockback: true
  bee_wings:
    # The duration in ticks of the slow falling effect
    duration: 100
    # The duration of the cooldown (in ticks)
    cooldown-duration: 300
  stinger:
    # Duration of the poison effect in ticks
    poison_duration: 60
    # Strength of the poison effect
    poison_strength: 0
  warped_fungus_eater:
    # Duration of the speed effect in ticks
    speed_duration: 200
    # Strength of the speed effect
    speed_strength: 0
    # Amount to increase hunger level by
    food_increase: 1
    # Amount to increase saturation by
    saturation_increase: 1.0
  sly:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  timid_creature:
    # Range to check for players in
    range: 8.0
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  pillager_aligned:
    # Whether Iron Golems should attack the player
    golems_attack: true
  small_weak:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  small_weak_knockback:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  surface_slowness:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  guardian_spikes:
    # Chance for thorns to damage attackers
    thorns_chance: 0.75
  prismarine_skin:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  frigid_strength:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  better_berries:
    # The amount to increase the food given by Sweet Berries by
    food_increase: 2
    # The amount to increase the saturation given by Sweet Berries by
    saturation_increase: 1.0
  wolf_body:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  item_collector:
    # Increased pickup range radius
    radius: 2.5
  better_potions:
    # The amount to multiply a potion's duration by
    duration_multiplier: 2.0
  elder_magic:
    # The range of the effect
    range: 5.0
    # The duration in ticks of the effect
    mining_fatigue_duration: 600
    # The strength of the effect
    mining_fatigue_strength: 1
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  elder_spikes:
    # Chance for thorns to damage attackers
    thorns_chance: 0.75
  summon_fangs:
    # The number of fangs to summon
    fang_count: 16
    trigger:
      # The action (e.g. LEFT_CLICK, RIGHT_CLICK) that triggers this ability
      # You can see a full list of actions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      type: LEFT_CLICK
      # The conditions (e.g. EMPTY_HAND, NO_BLOCK) that are required to trigger this ability
      # You can see a full list of conditions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      conditions:
      - EMPTY_HAND
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  full_moon:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  full_moon_health:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  full_moon_attack:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  wolf_pack:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  wolf_pack_attack:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  zombie_hunger:
    # The amount to multiply exhaustion by
    exhaustion_multiplier: 1.5
  melting:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  melting_speed:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  wolf_howl:
    # Range to check for wolves
    range: 5.0
    # The duration of the cooldown (in ticks)
    cooldown-duration: 900
  trident_expert:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 400
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  flower_power:
    # Range to check for flowers in
    range: 3
    # Required number of flowers
    required_flowers: 3
    # Strength of the regeneration
    regeneration_strength: 0
  lava_walk:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  split:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  potion_action:
    # Duration of the potion effect in ticks
    potion_duration: 200
    # Strength of the potion effect (only affects speed)
    potion_strength: 0
    trigger:
      # The action (e.g. LEFT_CLICK, RIGHT_CLICK) that triggers this ability
      # You can see a full list of actions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      type: LEFT_CLICK
      # The conditions (e.g. EMPTY_HAND, NO_BLOCK) that are required to trigger this ability
      # You can see a full list of conditions at https://starshooter.gitbook.io/origins-reborn/configuration#triggerable-ability-configuration
      conditions:
      - EMPTY_HAND
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
monsterorigins:
  explosive:
    # Whether the explosion should break blocks
    break_blocks: true
    # Whether the explosion should cause fire
    cause_fire: false
    # How much damage the player should take when creating an explosion
    health_lost: 8
    # The duration of the cooldown (in ticks)
    cooldown-duration: 300
  fear_cats:
    # How strong the weakness effect should be
    weakness_strength: 0
  metamorphosis_temperature:
    # The speed at which temperature should change
    speed_multiplier: 1
  sense_movement:
    # The radius things should glow within
    glow_range: 16
    # Whether to make invisible players show up
    see_invisible: true
  double_health:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  double_damage:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  sonic_boom:
    # The amount of damage the Sonic Boom should deal
    damage_dealt: 15
    # The duration of the cooldown (in ticks)
    cooldown-duration: 600
  trident_expert:
    # The duration of the cooldown (in ticks)
    cooldown-duration: 400
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  zombie_hunger:
    # The amount to multiply exhaustion by
    exhaustion_multiplier: 1.5
  apply_wither_effect:
    # Strength of the wither effect
    strength: 0
    # Duration in ticks of the wither effect
    duration: 200
  slowness_arrows:
    # The duration of the weakness effect in ticks
    weakness_duration: 600
    # The strength of the weakness effect
    weakness_strength: 0
  apply_hunger_effect:
    # Strength of the hunger effect
    effect_strength: 0
    # Duration in ticks of the hunger effect
    effect_duration: 200
  skeleton_body:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  slowness:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  land_slowness:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  heat_slowness:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  better_aim:
    # Divergence of arrows fired
    divergence: 0.0
  cold_slowness:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: MULTIPLY_SCALAR_1
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  better_gold_armour:
    attribute-modifier:
      # The operation to use ('ADD_SCALAR', 'ADD_NUMBER' or 'MULTIPLY_SCALAR_1')
      operation: ADD_NUMBER
      # A mathematical expression used to modify the attribute, where 'x' is a variable representing the default modifier.
      # Example: 'x * 3' will triple the value used in the modifier.
      expression: x
  better_gold_weapons:
    # Multiplier for damage done with golden tools
    damage_multiplier: 2.5
  zombified_piglin_allies:
    # Range to check for Zombified Piglins to attack a target
    range: 32
  super_bartering:
    # The minimum number of extra barters
    minimum_amount: 1
    # The maximum number of extra barters
    maximum_amount: 3
